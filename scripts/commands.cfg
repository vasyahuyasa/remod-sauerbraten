// exec all .cfg in directory
execdir = [
	dir = $arg1
	loopfiles f $dir cfg [
		file = (format "%1/%2.cfg" $dir $f) 
		echo (format "[remod] executing '%1'" $file)
    	exec $file
 	]	
]

execdir "scripts/commands"
execdir "scripts/irccommands"
execdir "scripts/other"
