// register this command only if flagrun module is loaded (and cmd_flagrun function exists). See scripts/modules/flagrun.cfg
if (identexists "cmd_flagrun") [
	registercommand "flagrun" cmd_flagrun 1 "|ww" "flagrun (mode) (map) ^f1Show best flagrun result for specified mode and map (or for current ones)."
	
]

