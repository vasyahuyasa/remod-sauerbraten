// change map and mode
cmd_map = [
	mode = (getmode)
	if (!=s $arg3 "") [
		if (= (isinlist $MODENAMES $arg3) 1) [
			mode = (indexof $MODENAMES $arg3)
		]
	]
	mapmode $arg2 $mode
]

registercommand "map" cmd_map 2 "w|w" "map [map] [mode]. ^f1Change server map and mode"  

