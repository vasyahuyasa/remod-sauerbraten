// register this command only if stats module is loaded (and irccmd_stats function exists). See scripts/modules/stats.cfg
if (identexists "irccmd_stats") [
	irc_registercommand "stats" irccmd_stats 0 "|c" "stats [cn]. Show game or player info"
]