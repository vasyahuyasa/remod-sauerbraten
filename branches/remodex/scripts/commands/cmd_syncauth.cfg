// register this command only if syncauth module is loaded (and cmd_syncauth funciton exists). See scripts/modules/syncauth.cfg
if (identexists "cmd_syncauth") [
	registercommand "syncauth" cmd_syncauth 3 "" "syncauth ^f1Reload authkeys"
]