// show player stats, -1 all

// register this command only if stats module is loaded (and cmd_stats function exists). See scripts/modules/stats.cfg
if (identexists "cmd_stats") [
	registercommand "stats" cmd_stats 1 "|p" "stats [cn] ^f1Shows game statistics for player cn for all players if passed -1"

]