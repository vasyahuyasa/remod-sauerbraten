// stop server
cmd_halt = [
	if (=s $arg2 1) [
		halt 0
	] [
		pm $arg1 "^f1This command will stop the server, if you want proceed type #halt 1"
	]
]

registercommand "halt" cmd_halt 3 "|b" "halt ^f1Stop server"
